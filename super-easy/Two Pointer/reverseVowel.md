We will always prefer two pointer approach over brute force approach because it is more efficient and easier to implement. 

Searching on Set is always a better approach than searching on List because Set has O(1) time complexity for lookups while List has O(n) time complexity.

Another good improvemnt here like runnnig internal while loop until smaller index is less than higher index and on every iteration don't forget to update index. This adds more efficiency to the code and reduces the time complexity.