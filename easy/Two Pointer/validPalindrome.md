- Use two pointer
- Here we don't have to reverse the string, we just need to check if it is a palindrome or not. So no need to convert sting to array.

- Need to do compare only when alphanumeric character is found. If not, we need to move the pointer to next index.
